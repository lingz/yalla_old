<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :password %><br />
    <%= f.text_field :password %>
  </div>
  <div class="field">
    <%= f.label :display_image %><br />
    <%= f.text_field :display_image %>
  </div>
  <div class="email">
    <%= f.label :email %><br />
    <%= text_field_tag(:email) %>
  </div>
  <div class="school">
    <%= f.label :school %>
    <%= f.select :school, School.all.map(&:name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
